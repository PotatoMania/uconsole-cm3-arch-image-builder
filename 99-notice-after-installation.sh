#!/bin/bash

echo "\
A simple ArchLinux image is created. You might still need to
setup timezone, locale, etc..
I still recommend to install the OS yourself, so you know 
the whole process at least.

I've done a few configurations that I think useful. But they
may not suit your need. Use with caution.

The image is ${WORKING_DIR}/${IMAGE_NAME},
and the compressed one is in the same folder.
"